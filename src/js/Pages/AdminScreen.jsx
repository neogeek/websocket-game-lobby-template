import React from 'react';
import { Box, Paragraph } from 'grommet';

const AdminScreen = ({ data, send }) => {
    return (
        <Box direction="column">
            <Paragraph size="xxlarge">Admin Screen</Paragraph>
        </Box>
    );
};

export default AdminScreen;
